cargo run register --service-name dev-portal-service --service-port 22 --forwarding-port 9090
cargo run list


kube-rs migration — kill the polling, thin client for real
Single shared tokio runtime — thread count drops dramatically
Streaming logs — lower latency, no poll timer
kube-rs port-forward in daemon — supervision machinery gone
egui repaint on demand — battery/heat fix
Terminal via kube-rs exec — eventually, after the rest stabilizes